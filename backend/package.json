{
  "name": "backend-json-server",
  "version": "1.0.0",
  "description": "JSON Server backend for serving db.json",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server db.json --host 0.0.0.0 --port 3000 --static ./public",
    "local": "json-server db.json --port 3000"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
